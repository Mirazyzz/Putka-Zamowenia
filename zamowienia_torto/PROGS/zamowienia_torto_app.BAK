****************
* Main Program *
****************
 
SET TALK OFF && Turn off service messages
SET DATE TO GERMAN && Set date format to German
SET CENTURY ON && Set full year on
SET PATH TO TO LIBS, PRG, MENUS, DATA, REPORTS, FORMS && Add paths

DO FORM glownaforma && Main form to be opened at the begining